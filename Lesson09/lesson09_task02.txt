# github link - https://github.com/KozhevnikovSpb/synergy_python.git

listN1 = map(int, input("¬ведите числа дл€ первого сипска через пробел: ").split())     # «адаем список один
listN2 = map(int, input("¬ведите числа дл€ второго списка через пробел: ").split())     # «адаем список два

setN1 = set(listN1)                                     # ѕреобразуем в множество список один
setN2 = set(listN2)                                     # ѕреобразуем в множество список два

setN = setN1.intersection(setN2)                        # ѕолучаем новое множество, в котором только повтор€ющиес€ числа в обоих множествах

print(f"ќдинаковых чисел в списках = {len(setN)}")      # ¬ыводим размер, полученного множества или количество одинаковых чисел


